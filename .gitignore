.DS_Store
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
node_modules/
*.tsbuildinfo
.build/

dsh-plugin-desktop/dist/
dsh-plugin-desktop/lib/
dsh-community-market/lib/

.env.yarn

# MVP WebApp secrets. Keep only the documented empty template in Git.
.env
.env.*
!.env.example

# DSH/runtime state must live on persistent volumes, never in source control.
.dsh/
.dsh-*/
.mvp-runtime/
.local-volumes/

# Generated startup/bootstrap material may contain bearer tokens.
*.bootstrap-url
*.startup-url
